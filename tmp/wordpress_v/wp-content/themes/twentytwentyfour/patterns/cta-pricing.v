import rt

pub fn init_wp_content_themes_twentytwentyfour_patterns_cta_pricing_php() {
	// unsupported statement: Stmt_Nop
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Pricing Table'),
		rt.new_string('Name for the pricing pattern'), rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Our Services'),
		rt.new_string('Sample heading for pricing pattern'), rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [
		rt.new_string('We offer flexible options, which you can adapt to the different needs of each project.'),
		rt.new_string('Sample description for a pricing table'),
		rt.new_string('twentytwentyfour'),
	]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Free'),
		rt.new_string('Sample heading for the first pricing level'),
		rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('$0'),
		rt.new_string('Sample price for the first pricing level'),
		rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('wp_kses_post', [
		rt.call_function('_x', [
			rt.new_string('Access to 5 exclusive <em>Études Articles</em> per month.'),
			rt.new_string('Feature for pricing level'),
			rt.new_string('twentytwentyfour'),
		]),
	]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Weekly print edition.'),
		rt.new_string('Feature for pricing level'), rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('wp_kses_post', [
		rt.call_function('_x', [
			rt.new_string('Exclusive access to the <em>Études</em> app for iOS and Android.'),
			rt.new_string('Feature for pricing level'),
			rt.new_string('twentytwentyfour'),
		]),
	]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Subscribe'),
		rt.new_string('Button text for the first pricing level'),
		rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Connoisseur'),
		rt.new_string('Sample heading for the second pricing level'),
		rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('$12'),
		rt.new_string('Sample price for the second pricing level'),
		rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('wp_kses_post', [
		rt.call_function('_x', [
			rt.new_string('Access to 20 exclusive <em>Études Articles</em> per month.'),
			rt.new_string('Feature for pricing level'),
			rt.new_string('twentytwentyfour'),
		]),
	]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Weekly print edition.'),
		rt.new_string('Feature for pricing level'), rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('wp_kses_post', [
		rt.call_function('_x', [
			rt.new_string('Exclusive access to the <em>Études</em> app for iOS and Android.'),
			rt.new_string('Feature for pricing level'),
			rt.new_string('twentytwentyfour'),
		]),
	]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Subscribe'),
		rt.new_string('Button text for the second pricing level'),
		rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Expert'),
		rt.new_string('Sample heading for the third pricing level'),
		rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('$28'),
		rt.new_string('Sample price for the third pricing level'),
		rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('wp_kses_post', [
		rt.call_function('_x', [
			rt.new_string('Exclusive, unlimited access to <em>Études Articles</em>.'),
			rt.new_string('Feature for pricing level'),
			rt.new_string('twentytwentyfour'),
		]),
	]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Weekly print edition.'),
		rt.new_string('Feature for pricing level'), rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('wp_kses_post', [
		rt.call_function('_x', [
			rt.new_string('Exclusive access to the <em>Études</em> app for iOS and Android'),
			rt.new_string('Feature for pricing level'),
			rt.new_string('twentytwentyfour'),
		]),
	]))
	// unsupported statement: Stmt_InlineHTML
	rt.echo_val(rt.call_function('esc_html_x', [rt.new_string('Subscribe'),
		rt.new_string('Button text for the third pricing level'),
		rt.new_string('twentytwentyfour')]))
	// unsupported statement: Stmt_InlineHTML
}
